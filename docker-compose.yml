version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: postgres-container
    environment:
      POSTGRES_DB: dogdb
      POSTGRES_USER: primea
      POSTGRES_PASSWORD: primea
    ports:
      - "5432:5432"
#    volumes:
#      - postgres_data:/var/lib/postgresql/data

#volumes:
#  postgres_data:
#    driver: local
#    driver_opts:
#      type: none
#      o: bind
#      device: "C:\Users\timmytoe\OneDrive\Skrivbord\postgres_persistent_path"